addons:
  sonarcloud:
    organization: "ahmettugur"
    token:
      secure: "travis encrypt 860ff1301b4b62c2db157ff803410d3a7150ff97" # encrypted value of your token

script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - sonar-scanner
